name: GitHub Pages

on:
  release:
    types: [created]

jobs:
  deploy:
     steps:
      - name: Deploy
      uses: peaceiris/actions-gh-pages@v3
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}

